# Storybook

A [Storybook](https://storybook.js.org/) application for developing and showcasing the components and features of the `react-async-modal-hook` library.

> It does not cover testing. This is handled in the [playwright app](../playwright).
